<UserControl x:Class="SteamTools.SelectSteamInstallLocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SteamTools"
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Background>
        <SolidColorBrush Color="Black" Opacity="0.85" />
    </UserControl.Background>
    <Border BorderThickness="3" BorderBrush="#FFFFFFFF" HorizontalAlignment="Center"  VerticalAlignment="Center">
        <Border.Background>
            <SolidColorBrush Color="Black" Opacity="0.85" />
        </Border.Background>
        <ListBox Name="Items" Style="{DynamicResource ListBoxStyle}" MouseDoubleClick="OnEnter">
        <!--<ListBox x:ConnectionId="1" Name="Items" Style="{DynamicResource ListBoxStyle}">-->
            <!--<ListBox.Triggers>
                <EventTrigger EventName="MouseDoubleClick">
                    <ActionMessage MethodName="OnEnter" />
                </EventTrigger>
            </ListBox.Triggers>-->
        </ListBox>
    </Border>
</UserControl>